apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Rede bridge padrão do compose - Grafana e Prometheus estão no mesmo projeto/rede, então
    # se resolvem pelo nome do serviço via DNS interno do Docker.
    url: http://prometheus:9090
    isDefault: true
    editable: false
